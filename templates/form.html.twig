{% extends 'partials/base.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col-sm-12">
            <h1 class="display-1">{{ page.title }}</h1>

            {{ content|raw }}
        </div>
    </div>

    <div class="row">
    {% include "forms/form.html.twig" %}
    </div>
{% endblock %}
