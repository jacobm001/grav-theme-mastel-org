<div class="card flex-md-row mb-4 box-shadow h-md-250">
	<div class="card-body d-flex flex-column align-items-start">
		<p class="card-title mb-2">{{ page.title }}</p>
		<p class="card-subtitle mb-2">{{ page.date|date("Y-m-d") }}</p>

		<p class="card-text mb-auto">{{ page.summary }}</p>

		<a href="{{ page.url }}">Read All</a>
	</div>
</div>
